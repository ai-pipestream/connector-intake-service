/**
 * Connector intake: accepts documents and crawl sessions from connectors, validates identity,
 * resolves Tier&nbsp;1 configuration, persists to the repository when configured, and hands
 * work to the engine for pipeline routing.
 */
package ai.pipeline.connector.intake;
